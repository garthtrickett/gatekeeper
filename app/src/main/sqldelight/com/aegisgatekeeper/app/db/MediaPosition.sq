CREATE TABLE IF NOT EXISTS MediaPosition (
    mediaId TEXT NOT NULL PRIMARY KEY,
    positionSeconds REAL NOT NULL
);

selectAll:
SELECT * FROM MediaPosition;

insert:
INSERT OR REPLACE INTO MediaPosition(mediaId, positionSeconds) VALUES (:mediaId, :positionSeconds);
